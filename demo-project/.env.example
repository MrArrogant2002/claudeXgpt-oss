# Copy to .env and adjust. These are demo PLACEHOLDERS — not real secrets.
NIMBUS_DB=:memory:
NIMBUS_MAX_URL_LENGTH=2048
NIMBUS_API_BASE=http://localhost:8000

# Example only — never commit real credentials.
NIMBUS_ADMIN_TOKEN=changeme-local-only
