[project]
name = "nimbus-api"
version = "0.4.2"
description = "Nimbus URL shortener — API service (framework-free, stdlib only)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
